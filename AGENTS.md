# Working instructions

This repository is Falling branch S1: safe removal of a cooperative controller while preserving essential service. Start with README.md, STATUS.md, and work_orders/CURRENT.md.

## Preserve

- Keep the original root LICENSE unchanged.
- Treat both dated checkpoint directories as immutable imported records. Add corrections in a new dated note and update current navigation/status; do not silently rewrite a historical proof or report.
- Do not upload the fiction, private conversation transcripts, or third-party papers.
- Keep passive fixed obligations, active fixed-rate preparation, and proportional-invalidation countermodels distinct.

## Scientific standard

The objective is a publishable, theorem-led contribution with a clear novelty claim and a small local verification burden. No large network simulations or neural training are required or authorized by the current work order.

Distinguish written proof, finite checks, operational evidence, source-derived claims, and novelty assessment. Never describe an unreviewed theorem as externally certified. Do not relabel network calculus, fallback recoverability, or migration scheduling as a new general AI-shutdown law.

Read the full relevant primary source before claiming an exact overlap or absence. Record model assumptions, quantifiers, theorem/equation/section references, and what was actually inspected. Keep uninspected leads marked as such.

Maintain all-request-time readiness, paid initialization, independent receiver accounting, and the difference between cooperative grace and immediate failure. Do not assume heterogeneous serial optimality from the homogeneous proof. Keep countermodels visible.

## Reproduction and writes

Run `python verify.py` without optimization. The runner copies checkpoints before executing their original scripts; it must not mutate historical files. Add new checks separately and state what they can and cannot establish.

Read the current remote branch before committing, preserve unrelated changes, use a non-forced update, and verify the resulting repository contents. A tested local file is not a committed file until the write succeeds.
